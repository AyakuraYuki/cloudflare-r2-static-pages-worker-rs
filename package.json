{
  "name": "cloudflare-r2-static-pages-worker-rs",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "dev-remote": "wrangler dev --remote"
  },
  "devDependencies": {
    "wrangler": "^3.5.1"
  }
}
